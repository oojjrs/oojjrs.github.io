interface:
  display_name: "oojjrs Image-First Art Workflow"
  short_description: "Imagegen-first art workflow with local post-processing tools"
  default_prompt: "Use $oojjrs-image-first-art-workflow: generate the first art pass with imagegen, never call imagegen only for previews, keep temporary/intermediate files under $Trash, use deterministic second-pass tools, and preserve required Unity metadata without creating or modifying it."
