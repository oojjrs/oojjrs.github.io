interface:
  display_name: "oojjrs Guidelines"
  short_description: "Load shared work rules and reuse text-format validation tools."
  default_prompt: "Use $oojjrs-guidelines before actual repository work and reuse its text-format checker for edited files."
