interface:
  display_name: "oojjrs Guidelines"
  short_description: "Reload shared rules at task and finish phases."
  default_prompt: "Use $oojjrs-guidelines at the start of every thread and immediately before every command or tool call; do not reuse a task-start read for validation, stage, commit, push, handoff, or final reporting, and re-read $oojjrs-project-finish-work after the last edit before those finish actions."
