interface:
  display_name: "oojjrs Guidelines"
  short_description: "Load canonical work rules once per task thread"
  default_prompt: "Use $oojjrs-guidelines to read the canonical shared work rules once, report the active SHA when available, and route only the workflow skills this task needs."
