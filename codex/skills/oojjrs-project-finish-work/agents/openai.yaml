interface:
  display_name: "oojjrs Project Finish Work"
  short_description: "Audit version policy, validation, and commit"
  default_prompt: "Use $oojjrs-project-finish-work after actual edits or for scoped Git completion work to perform the mandatory version-policy check before every authorized commit, stabilize the diff, and explicitly decide every finish gate."
