interface:
  display_name: "Image-first Art Workflow"
  short_description: "Imagegen-first art creation rule"
  default_prompt: "Use $image-first-art-workflow: generate the first art pass with imagegen, then use local tools only for post-processing such as sizing, slicing, cleanup, or formatting."
