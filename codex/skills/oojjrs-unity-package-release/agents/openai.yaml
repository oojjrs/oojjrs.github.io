interface:
  display_name: "oojjrs Unity Package Release"
  short_description: "Apply scoped UnityO version policy"
  default_prompt: "Use $oojjrs-unity-package-release to classify changed UnityO packages, verify only affected release metadata, and run builds or tests only when explicitly requested with an independent expected result."
