interface:
  display_name: "oojjrs Unity Package Release"
  short_description: "Finish Unity package release bundles"
  default_prompt: "Use $oojjrs-unity-package-release to inspect release readiness or synchronize version, metadata, docs, and validation exactly as requested for an explicit Unity package release bundle."
