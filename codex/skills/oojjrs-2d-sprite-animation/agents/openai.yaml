interface:
  display_name: "oojjrs 2D Sprite Animation"
  short_description: "Build clean 2D sprite sheets from layered parts."
  default_prompt: "Use $oojjrs-2d-sprite-animation to create a clean sprite sheet and preview from actual frames, preserve required Unity metadata without creating or modifying it, and never use preview-only imagegen calls."
