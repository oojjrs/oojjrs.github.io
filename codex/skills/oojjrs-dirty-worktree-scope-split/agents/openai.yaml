interface:
  display_name: "oojjrs Dirty Worktree Scope Split"
  short_description: "Isolate overlapping work in dirty repositories"
  default_prompt: "Use $oojjrs-dirty-worktree-scope-split to isolate overlapping requested work from unrelated dirty files, and stage or commit only when the current request authorizes it."
