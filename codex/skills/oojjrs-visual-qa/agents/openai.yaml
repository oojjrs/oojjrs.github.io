interface:
  display_name: "oojjrs Visual QA"
  short_description: "Verify visual surfaces and layouts"
  default_prompt: "Use $oojjrs-visual-qa to inspect rendered output, screenshots, responsive layout, image loading, and static fallbacks when browser checks are blocked."
