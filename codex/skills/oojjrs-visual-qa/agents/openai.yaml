interface:
  display_name: "oojjrs Visual QA"
  short_description: "Verify only affected rendered surfaces"
  default_prompt: "Use $oojjrs-visual-qa only when rendered evidence is requested or changed layout cannot be established statically, inspect the affected viewport and state, and derive QA previews from actual surfaces or assets."
