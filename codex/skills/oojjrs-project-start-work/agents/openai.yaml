interface:
  display_name: "oojjrs Project Start Work"
  short_description: "Isolate repository mutation scope once"
  default_prompt: "Use $oojjrs-project-start-work once before the first authorized local file or Git-index mutation to protect existing work and select only the required domain workflow."
