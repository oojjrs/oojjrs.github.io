interface:
  display_name: "oojjrs Project Start Work"
  short_description: "Gate repository and Git mutations by scope"
  default_prompt: "Use $oojjrs-project-start-work immediately before the first authorized local file, index, or commit mutation to isolate scope and select only the required domain workflow."
