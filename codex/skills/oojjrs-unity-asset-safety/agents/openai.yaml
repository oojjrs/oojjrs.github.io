interface:
  display_name: "oojjrs Unity Asset Safety"
  short_description: "Change Unity assets through the official CLI"
  default_prompt: "Use $oojjrs-unity-asset-safety to change Unity assets only through the official Unity CLI, let Unity generate metadata, apply the planning document's UI resolution or FHD default, and verify sprite and atlas importer settings by readback."
