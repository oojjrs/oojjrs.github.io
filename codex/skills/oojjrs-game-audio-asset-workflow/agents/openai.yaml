interface:
  display_name: "oojjrs Game Audio Asset Workflow"
  short_description: "Route SFX creation, sourcing, and project audio"
  default_prompt: "Use $oojjrs-game-audio-asset-workflow to route SFX creation directly to ElevenLabs, source existing audio only when I explicitly ask, and never process audio unless I explicitly request the edit."
