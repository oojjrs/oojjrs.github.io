interface:
  display_name: "oojjrs README Doc Generation"
  short_description: "Write GitHub-facing README docs"
  default_prompt: "Use $oojjrs-readme-doc-generation only when README or public entry documentation is the primary deliverable; when another primary domain needs an incidental README edit, let that domain perform it without loading this standalone skill."
