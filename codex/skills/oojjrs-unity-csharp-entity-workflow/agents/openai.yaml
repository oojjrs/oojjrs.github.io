interface:
  display_name: "oojjrs Unity C# Entity Workflow"
  short_description: "Add complete Unity entity families safely"
  default_prompt: "Use $oojjrs-unity-csharp-entity-workflow to classify the entity as Record-only, Data-and-Record, or Data-only, then add only the required DataBuilder, Manager, transport, and client integration."
